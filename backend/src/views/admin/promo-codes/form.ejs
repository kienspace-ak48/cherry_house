<div class="admin-page-head d-flex flex-wrap justify-content-between align-items-start gap-3 mb-4">
  <div>
    <h2><%= mode === 'edit' ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá' %></h2>
    <p class="text-body-secondary mb-0 small">Mã áp dụng toàn hệ thống — validate tại checkout.</p>
  </div>
  <a href="/admin/promo-codes" class="btn btn-outline-secondary btn-sm">← Danh sách</a>
</div>

<%- include('../../partials/admin/promoCodeForm', {
  formAction: mode === 'edit' ? '/admin/promo-codes/' + promo.id : '/admin/promo-codes',
  submitLabel: mode === 'edit' ? 'Cập nhật' : 'Tạo mới',
  mode: mode,
}) %>

<script src="/admin/js/admin-promo-form.js"></script>
