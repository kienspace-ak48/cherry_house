<div class="admin-page-head d-flex flex-wrap justify-content-between align-items-start gap-3 mb-4">
  <div>
    <h2><%= mode === 'edit' ? 'Sửa loại phòng' : 'Thêm loại phòng' %></h2>
    <p class="text-body-secondary mb-0 small">
      Nội dung này quyết định gallery, tiện nghi và mô tả trên trang chi tiết phòng.
    </p>
  </div>
  <a href="/admin/room-types" class="btn btn-outline-secondary btn-sm">← Danh sách</a>
</div>

<%- include('../../partials/admin/roomTypeForm', {
  formAction: mode === 'edit' ? '/admin/room-types/' + roomType.id : '/admin/room-types',
  submitLabel: mode === 'edit' ? 'Cập nhật' : 'Tạo mới',
  mode: mode,
}) %>

<script src="/admin/js/admin-room-type-form.js"></script>
