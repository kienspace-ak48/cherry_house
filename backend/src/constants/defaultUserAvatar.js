/** Avatar mặc định cho user đăng ký email (file tĩnh ở backend/public + frontend/public). */
const DEFAULT_USER_AVATAR_URL = '/default-user-avatar.svg';

module.exports = { DEFAULT_USER_AVATAR_URL };
